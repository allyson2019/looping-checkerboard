function setup() {
  createCanvas(500,500);
}
function draw(){
     background(0);
     fill(250);
     
    var x = 40
    var count = 0
}

while (x < count) {
rect (x, x, 25, 25)
}

if (count % 2 === 0){
fill (orange)
else fill (black)
    
}